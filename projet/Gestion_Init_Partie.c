#include <stdio.h>
#include <stdlib.h>
#include "libraryProjet.h"

void Distribuer_Cartes(TJoueur * listeJoueur, TCarte * listeCartes, int nbCartes, int nbJoueur)
{
    int nbCartesLocales,nbJoueurLocal = 0;

    TPilelem aux;
    aux =


    for (nbCartesLocales = 0; nbCartesLocales < 5*nbJoueur; nbCartesLocales += nbJoueur)
    {
        for(nbJoueurLocal = 0; nbJoueurLocal < nbJoueur; nbJoueurLocal ++)
        {
            piocher()
            listeJoueur[nbJoueurLocal].main. =
        }
    }

}
